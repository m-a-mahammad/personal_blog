<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Update Page</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: beige;
      margin: 0;
      padding: 20px;
    }

    .wrapper {
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .blog {
      background-color: #fff;
      padding: 25px;
      padding-right: 47px;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      width: 600px;
      max-width: 90%;
    }

    .blog input,
    .blog textarea {
      display: block;
      width: 100%;
      margin-bottom: 15px;
      padding: 10px;
      font-size: 16px;
      border: 1px solid #ccc;
      border-radius: 5px;
    }

    .blog input:focus,
    .blog textarea:focus {
      outline: none;
    }

    #editBtn {
      background-color: #d55959;
      color: white;
      border: none;
      padding: 12px 20px;
      font-size: 16px;
      border-radius: 5px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }
  </style>
</head>

<body>
  <div class="wrapper">
    <div id="editForm" class="blog">
      <input id="titleInp" type="text" class="name" value="<%= data.title %>">
      <input id="dateInp" type="date" class="date" value="<%= data.date %>">
      <textarea id="contentInp" rows="20" cols="100" class="content"><%= data.content %></textarea>
      <button id="editBtn">Update</button>
    </div>
  </div>
</body>

<script>
  const titleInp = document.getElementById("titleInp");
  const dateInp = document.getElementById("dateInp");
  const contentInp = document.getElementById("contentInp");
  const editBtn = document.getElementById("editBtn");

  window.addEventListener("DOMContentLoaded", () => {
    editBtn.addEventListener("click", async () => {
      const res = await fetch("http://localhost:3000/edit/<%= data.id %>", {
        method: "PATCH",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          title: titleInp.value,
          date: new Date(dateInp.value).toLocaleDateString("en-US", {
            year: "numeric",
            month: "long",
            day: "numeric"
          }),
          content: contentInp.value
        })
      })

      if (res.ok) {
        window.location.href = "/admin";
      } else {
        window.location.href = "/login";
      }
    })
  })
</script>

</html>
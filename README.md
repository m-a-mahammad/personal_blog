# 📰 Simple Blog CMS with Admin Panel

نظام إدارة محتوى بسيط (CMS) مبني بـ Node.js وExpress، يسمح بعرض وتحرير وإنشاء وحذف المقالات، مع صلاحيات دخول للمشرفين فقط. يتم تخزين البيانات في ملف JSON محلي، وتُعرض الصفحات باستخدام EJS.
## 🚀 الميزات

- 👥 تسجيل دخول للمشرفين فقط
- 🏠 عرض المقالات العامة في صفحة `/home`
- 📄 عرض مقال مفرد عبر `/article/:id`
- 🛠️ لوحة تحكم للمشرفين عبر `/admin`
- ✏️ تعديل المقالات عبر `/edit/:id`
- 🆕 إنشاء مقال جديد عبر `/new`
- ❌ حذف مقال عبر `/delete/:id`
- 🔐 حماية جميع العمليات الإدارية بجلسة المشرف
- 📁 تخزين البيانات في `data.json`
## 🛠️ المتطلبات

- Node.js >= 18
- ملف `admin-users.json` يحتوي على بيانات تسجيل الدخول للمشرفين
- مجلد `src/views` يحتوي على ملفات EJS التالية:

```
404.ejs
admin.ejs
article.ejs
edit.ejs
home.ejs
login.ejs
new.ejs
```
## 📦 التثبيت

```bash
npm install
```
## 🔐 إعداد المستخدمين الإداريين

أنشئ ملف `admin-users.json` في جذر المشروع:
## ▶️ التشغيل

```bash
npm run dev
```

السيرفر يعمل على: [http://localhost:3000](http://localhost:3000/)

## 🧪 نقاط الوصول

### 👥 تسجيل الدخول
- **GET /login** — صفحة تسجيل الدخول
- **POST /submit** — تحقق من بيانات المشرف وتخزين الجلسة
### 🏠 عرض المقالات
- **GET /home** — عرض جميع المقالات
- **GET /article/:id** — عرض مقال مفرد
### 🛠️ لوحة المشرف
- **GET /admin** — عرض جميع المقالات مع أدوات الإدارة
- **GET /edit/:id** — تعديل مقال
- **PATCH /edit/:id** — حفظ التعديلات
- **GET /new** — إنشاء مقال جديد
- **POST /new** — حفظ المقال الجديد
- **DELETE /delete/:id** — حذف مقال
### ❌ صفحة الخطأ
- **GET /404** — عرض صفحة الخطأ
- أي مسار غير معرف يتم توجيهه تلقائيًا إلى `/404`
## 📂 هيكل المشروع

```
├── src/
│	│   
│	├── views/               # ملفات EJS
│	│	├── home.ejs
│	│	├── article.ejs
│	│	├── admin.ejs
│	│	├── edit.ejs
│	│	├── new.ejs
│	│	├── login.ejs
│	│	└── 404.ejs
│	├── interfaces/
│	│	└── data.interface.ts
│	├── types/
│	│	└── sessions.d.ts
│	├── app.ts                 # الكود الرئيسي
├── data.json                # تخزين المقالات
├── admin-users.json         # بيانات المشرفين
├── tsconfig.json
├── package.json 
└── README.md
```

## 🧠 ملاحظات تقنية

- يتم استخدام `express-session` لتخزين بيانات الجلسة
- يتم التحقق من صلاحية المشرف قبل أي عملية تعديل أو حذف
- يتم تنسيق التاريخ تلقائيًا في صفحة التعديل بصيغة `yyyy-mm-dd`
- يتم عرض صفحة 404 لأي طلب غير موجود
